class UserController {
  async show(req, res) {
    const user = {};
    return user;
  }

  async store(req, res) {
    const user = {};
    return user;
  }
}

module.exports = new UserController();
